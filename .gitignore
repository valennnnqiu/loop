# 你自己的真实交易数据 — 永远不要提交
my-data.json
*.local.json
*-backup-*.json
loop-backup-*.json

# 设计草稿 / 测试用的对账单，含真实账号和成交记录
mockups/
*.csv
U*_*.pdf

# 本地测试数据
_testdata.json

# 本地开发服务器配置（各人环境不同）
.claude/launch.json

# OS
.DS_Store
